// let aom: [string, number] = ['Aom', 25];
//当赋值或访问一个已知索引的元素时，会得到正确的类型：
// let aom: [string, number];
// aom[0] = 'Aom';
// aom[1] = 25;

// aom[0].slice(1);
// aom[1].toFixed(2);

let aom: [string, number] = ['Aom', 22];
aom.push(33);